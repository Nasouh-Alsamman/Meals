import React from "react";

export default function Search() {
  

  return (
    <header className="header" >
      <h1 onClick={() => window.location.reload(false)}>AGS international</h1>
    </header>
  );
}
